import {performance} from 'node:perf_hooks';import {runConformance} from '../src/conformance.js';import {merkle} from '../src/runtime.js';
const N=1000;const t0=performance.now();for(let i=0;i<N;i++)runConformance();const t1=performance.now();const t2=performance.now();for(let i=0;i<N;i++)merkle(['a','b','c','d',String(i)]);const t3=performance.now();console.log(JSON.stringify({iterations:N,conformance_total_ms:+(t1-t0).toFixed(3),conformance_avg_ms:+((t1-t0)/N).toFixed(6),merkle_total_ms:+(t3-t2).toFixed(3),merkle_avg_ms:+((t3-t2)/N).toFixed(6),environment:{node:process.version,platform:process.platform,arch:process.arch}},null,2));
